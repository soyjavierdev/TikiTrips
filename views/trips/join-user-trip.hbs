<div class="container">
    <h2 class="page-title">You have joined the trip!</h2>
    <div class="row">
        {{#each passengers}}
        <p>User who have joined the trip </p>
        <div class="col-3 mb-5 mt-5">
            <div class="card" style="width: 18rem;">
                <a href="/users/{_id}}" class="btn btn-primary"><img src="{{profilePic}}" class="card-img-top"
                        alt="{{username}} profile picture"></a>
                <div class="card-body">
                    <h5 class="card-title">{{username}}</h5>
                    <p class="card-text">{{role}}</p>
                    <a href="/users/{{_id}}" class="btn btn-primary">Check profile</a>
                </div>
            </div>
        </div>
    </div>
    {{/each}}
</div>